# FORMAT_SPEC.md

本文件是从用户提供的《化工进展》论文模板格式（2026版）中抽取的格式规范。模板本身说明其由多篇文章拼接而成，内容可能不连贯，因此这里分为“明示硬规则”和“模板观测样式”。若发生冲突，优先级为：期刊最新官网规则 > 模板红字说明 > 模板正文观测样式 > 本 skill 的默认值。

## 1. 页面设置

- 纸张：A4。
- 上边距：2.5 cm。
- 下边距：2.0 cm。
- 左右边距：2.0 cm。
- 单栏。
- 44 行，每行 46 字。
- 单倍行距。
- 不排双栏。
- 不需要页眉页脚。

机器生成和检查必须同时核对模板 raw OOXML：

- `w:pgSz`：`w=11906`，`h=16838`。
- `w:pgMar`：`top=1418`，`bottom=1134`，`left=1134`，`right=1134`，`header=0`，`footer=0`，`gutter=0`。
- `w:docGrid`：`type=linesAndChars`，`linePitch=324`，`charSpace=4294967090`。
- Normal 样式观测：两端对齐，英文字母/数字 Times New Roman，中文宋体，`w:sz=21`，`w:szCs=24`。
- 段落默认行距：`w:spacing line=240 lineRule=auto before=0 after=0`。

## 2. 全文字体原则

- 投稿及修改稿中，字母及数字建议使用 Times New Roman。
- 常用中文字体：宋体、黑体、仿宋、楷体、Times New Roman。
- 其他字体尽量不要使用。
- 除矢量、矩阵等特殊符号外，字体均不要加粗。
- 物理量符号用斜体，计量单位用正体。
- 数字与单位之间不空格。
- 正文组合单位用斜线形式，如 `mL/(cm2·min)`。
- 图表中单位用指数形式，如 `mL·cm−2·min−1`。

## 3. 禁用或尽量避免的 Word 特性

全文尽量不要或减少使用：

- 文本框；
- 脚注；
- 尾注；
- 批注；
- 分节符；
- 超链接；
- 域代码关联的引用；
- 页眉页脚。

图片、公式等非表格内容不要放入表格或文本框。正文中插入方式为“嵌入型”“无文字环绕”。图片要去掉四周多余白边。

## 4. 头部信息

### 4.1 文章类型

- 单独一行。
- 文字录入，不要用图片。
- 从“综述与专论 / 研究开发 / 化工园区等”中选择。
- 模板观测：黑体，16 pt。

### 4.2 DOI

- 单独一行。
- 格式示例：`DOI：10.16085/j.issn.1000-6613.****-****`。
- 初投稿不必填写稿件编号，修改稿须填写。
- 模板观测：`DOI` 标签加粗。

### 4.3 中文题名

- 居中。
- 精炼醒目。
- 去掉“研究”“实验”“综述”等字样。
- 一般不超过 20 个字。
- 模板观测：16 pt，居中。

### 4.4 中文作者与单位

- 作者姓名之间用中文逗号隔开。
- 单位排在姓名下方。
- 单位名称用全称，著录到二级单位。
- 单位格式：`（1单位全称，省 市 邮编；2单位全称，省 市 邮编）`。
- 模板观测：单位 9 pt，居中。

### 4.5 中文摘要

- 使用“摘要：”，不用“提要”。
- 一般不出现公式。
- 首句一般去掉“本文”。
- 不使用第一人称。
- 不出现参考文献序号。
- 中文摘要应在 300 字以上。
- 摘要、前言、结论/结语不得简单重复。
- 模板观测：9 pt，左缩进约 0.74 cm。
- 模板 run 级观测：`摘要：` 标签为黑体，正文为楷体；字号 `w:sz=18`、`w:szCs=18`，字符间距 `w:spacing=1`。

### 4.6 中文关键词

- 使用“关键词：”。
- 一般 3～8 个。
- 词间用中文分号。
- 尽量至少从《化工进展》标准关键词库中选取 3 个标准关键词。
- 模板观测：9 pt，左缩进约 0.74 cm。
- 模板 run 级观测：`关键词` 标签为黑体，冒号及关键词内容为楷体；字号 `w:sz=18`、`w:szCs=18`，字符间距 `w:spacing=1`。

### 4.7 中图分类号 / 文献标志码 / 文章编号

- 中图分类号按《中国图书馆分类法》确定。
- 化学工程领域一般入 TQ 类别，可列多个。
- 文献标志码统一为 A。
- 文章编号作者不需填写。
- 模板 run 级观测：`中图分类号：`、`文献标志码：`、`文章编号：` 标签为黑体；分类号和值为楷体；字号 `w:sz=18`、`w:szCs=18`，字符间距 `w:spacing=1`。

## 5. 英文部分

### 5.1 英文题名

- 与中文题名对应。
- 略去题目中的冠词。
- 去掉 `Study on` 等字样。
- 模板观测：14 pt，加粗，居中。

### 5.2 英文作者

- 作者之间用英文逗号隔开。
- 姓所有字母大写。
- 名首字母大写。
- 名不用连字符。

### 5.3 英文单位

- 单位名称用全称，不用 `Lab.` 等缩写。
- 后接 `, 城市 邮编, 省, 国家名`。

### 5.4 英文摘要与关键词

- 英文摘要不少于中文摘要内容。
- 包括目的、方法、结果、结论。
- 首次出现缩写时给出全称。
- 使用 `Keywords:`，不要使用 `Key words:`。
- 英文关键词与中文关键词对应。
- 非专有名词小写。
- 词间用英文分号。
- 英文标点用半角格式，后加半空格。
- 模板 run 级观测：`Abstract:`、`Keywords:` 标签加粗；英文摘要 `w:szCs=18`、`w:spacing=1`；英文关键词标签 `w:szCs=21`，关键词内容 `w:szCs=18`，字符间距均为 `1`。模板部分英文 run 未直接写入 `w:rFonts/w:sz`，生成器显式写入有效 Times New Roman 字体和字号，检查器按 `STYLE_MAP.json` 合同执行。

## 6. 正文结构

### 6.1 引言

- 不写“引言”或“前言”两字。
- 不排序号。
- 尽量不用“我们”“本人”等第一人称。
- 建议内容：研究背景、已有研究及评论、为什么需要进一步研究、研究目的、本文工作、结果意义。

### 6.2 标题层级

- 层次编号用阿拉伯数字。
- 一级：`1  材料和方法`。
- 二级：`1.1  材料`。
- 三级：`1.1.1  等离子体诱导填孔接枝聚合原理`。
- 不用“一、”“（一）”等中文编号。
- 模板观测：一级标题 14 pt 仿宋；二级标题黑体，编号加粗；三级标题约正文大小。

### 6.3 正文段落

- 中文默认宋体。
- 英文和数字 Times New Roman。
- 模板观测：约 10.5 pt。
- 首行缩进约 0.74 cm。
- 两端对齐。
- 单倍行距。

## 7. 图

- 正文中必须有与图呼应的文字，叙述应与图结果相符。
- 图按全文出现顺序统一编号。
- 图题放在图下方。
- 图题、分图题、图注、图内文字，除专有名词外均用中文。
- 图宽：半栏图一般不大于 75 mm；通栏图一般不大于 150 mm。
- 流程图、设备图要合理、简洁，不列与正文无关内容。
- 注意流程图箭头走向。

### 7.1 坐标图

- 一律采用封闭图。
- 端线尽量取在刻度线上。
- 横、竖坐标必须垂直。
- 坐标刻度线疏密程度相近。
- 刻度线朝向图内。
- 一般去掉无数字对应的刻度线。
- 不用背景网格线。
- 标度数字尽量圆整，过大或过小时可用指数表示，如 `10^2`、`10^-2`。
- 图四边框大小不含坐标文字，一般高 4 cm、宽 6 cm。
- 线粗 0.5 磅。
- 文字大小 6 号。
- 图注各项间用分号，结束无标点。
- 图内空间较大时可将图注列在图内空白处。
- 坐标物理量尽量用符号表示，物理量与单位之间用斜线，单位用指数形式。

### 7.2 分图和照片图

- 分图用 `(a)`、`(b)` 区分。
- 分图题置于各分图下方。
- 照片图必须清晰、层次分明。
- 放大倍数或比例尺应清晰易辨。
- 图尽量采用黑白图；如有必要可采用彩色图。

## 8. 表

- 正文中必须有与表呼应的文字。
- 表按全文出现顺序统一编号。
- 表题放在表上方。
- 表题、表注、表内文字，除专有名词外均用中文。
- 表结构应简洁，具有自明性。
- 采用三线表。
- 表头物理量对应数据应纵向可读。
- 表内物理量尽量用符号表示。
- 物理量与单位之间用斜线，单位用指数形式。
- 模板观测：表题黑体 9 pt 居中，表内文字约 7.5 pt。

### 8.1 表注

- 综合性表注：不加括号的阿拉伯数字编号，数字前冠以“注：”。注文回行时左边顶格，每注末加句号。
- 与表内文字或数字对应的表注：表内右上角加 `①、②`，表注也以 `①、②` 引出。

## 9. 公式

- 公式按全文出现顺序统一编号。
- 编号使用半角括号加数字，如 `(1)`。
- 正文引用写作 `式(1)`。
- 物理量符号用斜体。
- 下脚一般为正体，变量为斜体。
- 使用 MathType 等公式编辑器。
- 不要用文本输入或 Word 自带公式编辑器。
- 避免以文字、图片形式插入。
- 不要放在表格、文本框中。

## 10. 结论 / 结语

- 研究性文章使用“结论”。
- 综述性文章一般使用“结语”。
- 在研究结果与讨论基础上总结重要论点。
- 建议包括：解释结果、联系研究目的/假设、阐明重要性、与已有研究比较、得出清晰结论。
- 每个结论需要总结证据。
- 可指出不足和展望。
- 不能简单重复摘要和引言。

## 11. 符号说明

- 按英文字母顺序排列。
- 同一字母先大写后小写。
- 希腊文接英文后排，也按字母顺序排列。
- 采用表格形式。
- 符号与说明间用二字线 `——`。
- 说明文字与单位间用逗号。
- 一个符号只代表一个物理含义，一个物理量只用一个符号表示。
- 有单位必须给出单位，尽量使用国际制标准单位。
- 物理量符号采用国家标准，如压力 `p`、温度 `T`，均用斜体。
- 矢量、张量、矩阵用粗斜体。

## 12. 参考文献

- 按文中引用先后顺序列出。
- 序号加方括号。
- 严格执行《化工进展》参考文献著录规则。
- 外文作者姓所有字母在文后列序中均大写。
- 正文中外文作者的姓只需首字母大写。
- 中文文献下对应给出英文。
- 标点符号用半角形式，注意间距。
- 文中引用参考文献不要采用域、超链接等形式关联，均应以文本形式录入。

## 13. EndNote/Zotero 策略

- 工作稿可以保留 EndNote/Zotero 域代码，便于管理引用。
- 提交稿必须另存为纯文本引用版本。
- 必须保留一份带域代码的备份稿，不要在唯一文档上直接破坏域代码。
